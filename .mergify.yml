pull_request_rules:
  - name: automatic merge for github-actions pull requests
    conditions:
      - author=github-actions[bot]

    actions:
      merge:
        method: merge

  - name: delete head branch after merge
    conditions:
      - merged
    actions:
      delete_head_branch: {yuque}