- let pageFillDescription = get_page_fill_description()
- let gptName = theme.post_head_ai_description.gptName
- let mode = theme.post_head_ai_description.mode
- let switchBtn = theme.post_head_ai_description.switchBtn
if (pageFillDescription && page.description)
  .post-ai-description
    .ai-title
      i.fa-solid.fa-newspaper
      .ai-title-text AI-摘要
      if (switchBtn)
        #ai-Toggle(title="CC/Tianli 模式切换") 切换
      i.fa-solid.fa-podcast(title="Tianli 模式下朗读摘要")
      i.fa-solid.fa-arrow-rotate-right(title="Tianli 模式下刷新摘要")
      #ai-tag
        if mode == "tianli"
          = "Tianli GPT"
        else
          = gptName + " GPT"
    .ai-explanation AI初始化中...
    .ai-btn-box
      .ai-btn-item 介绍自己 🙈
      .ai-btn-item 生成摘要 👋
      .ai-btn-item 相关推荐 📖
      .ai-btn-item 前往主页 🏠
      .ai-btn-item#go-tianli-blog 前往爱发电购买
    script(data-pjax src=url_for(theme.asset.ai_abstract_js))
